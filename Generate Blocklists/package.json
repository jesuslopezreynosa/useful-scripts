{
  "scripts": {
    "setup": "npm install",
    "generate": "npx ts-node generateBlockists.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}